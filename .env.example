# pieBot environment variables (local only)
#
# Copy to .env and fill values. NEVER commit .env.
#
# Provider dispatch (Stage 6B)
# - Base URL for OpenAI-compatible endpoint.
#   For OpenAI: https://api.openai.com
#   For local gateway: http://localhost:8000
OPENAI_BASE_URL=https://api.openai.com

# API key for provider (optional if your base URL is local and unauthenticated).
# For OpenAI this is required.
OPENAI_API_KEY=

# Optional: enable provider dispatch (future wiring; does nothing yet unless used by orchestrator)
PIEBOT_ENABLE_PROVIDERS=0
OPENMEMORY_API_KEY=