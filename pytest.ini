; pytest.ini configuration file for the project
; required to exclude vendor directories from test discovery

[pytest]
testpaths = tests
norecursedirs = vendor runtime .git .venv dist build node_modules
addopts = -ra
