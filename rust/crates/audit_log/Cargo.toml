[package]
name = "pie_audit_log"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
pie_common = { path = "../common" }
pie_audit_spec = { path = "../audit_spec" }
